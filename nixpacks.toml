# Nixpacks configuration for Railway deployment

[phases.setup]
nixPkgs = ["ffmpeg"]

[phases.install]
cmds = [
    "pip install uv",
    "uv pip install --system .",
]

[start]
cmd = "uvicorn xlikes_viewer.server:app --host 0.0.0.0 --port $PORT"
